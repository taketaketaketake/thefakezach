---
import { liveProjects } from "../data/live-projects";

const statusDisplayMap: Record<string, string> = {
  "live": "Live",
  "in-progress": "In Progress",
  "paused": "Paused",
};

const statusBadgeClass: Record<string, string> = {
  "live": "bg-paper2 text-ink border-ink",
  "in-progress": "bg-paper2 text-muted border-ink",
  "paused": "bg-paper2 text-muted border-ink",
};
---

<div class="flex flex-col gap-6">
  {liveProjects.map((p) => (
    <a
      href={`/live-projects/${p.slug}`}
      class="group relative grid grid-cols-1 md:grid-cols-[1fr_240px] gap-6 bg-paper2 backdrop-blur border border-ink rounded-2xl p-6 sm:p-7 text-ink no-underline hover:no-underline hover:border-ink transition-all duration-300"
    >
      <span
        class={`absolute top-4 right-4 px-3 py-1 text-[10px] font-bold uppercase tracking-wider rounded-full border ${statusBadgeClass[p.status]}`}
      >
        {statusDisplayMap[p.status]}
      </span>

      <div class="min-w-0">
        <div class="flex items-baseline gap-3 flex-wrap mb-2 pr-24">
          <span class="text-3xl leading-none">{p.emoji}</span>
          <h3 class="text-2xl font-bold text-ink">{p.title}</h3>
          <span class="bg-paper2 text-ink px-3 py-1 rounded-full text-xs font-medium">
            {p.year}
          </span>
        </div>

        <p class="text-muted text-sm font-medium mb-4">{p.tagline}</p>

        <p class="text-ink leading-relaxed mb-5">{p.summary}</p>

        <div class="grid grid-cols-2 sm:grid-cols-4 gap-3 mb-5">
          {p.metrics.slice(0, 4).map((m) => (
            <div class="bg-paper/60 border border-ink rounded-lg p-3">
              <div class="text-[10px] uppercase tracking-wider text-muted font-semibold mb-1 truncate">
                {m.label}
              </div>
              <div class="text-2xl font-extrabold text-ink leading-none mb-1">
                {m.value}
              </div>
              {m.hint && <div class="text-[10px] text-muted">{m.hint}</div>}
            </div>
          ))}
        </div>

        <span
          class="inline-flex items-center gap-2 bg-ink text-white px-4 py-2 rounded-lg font-bold text-xs uppercase tracking-wider transition-transform group-hover:scale-105"
        >
          Open Detail →
        </span>
      </div>

      <div class="flex items-center justify-center order-first md:order-none">
        {p.image ? (
          <img
            src={p.image}
            alt={p.title}
            class="w-full aspect-square object-cover rounded-xl border border-ink"
            style={p.imagePosition ? `object-position: ${p.imagePosition}` : undefined}
            loading="lazy"
          />
        ) : (
          <div
            class="w-full aspect-square flex items-center justify-center bg-paper2 border border-dashed border-ink rounded-xl text-7xl"
          >
            {p.emoji}
          </div>
        )}
      </div>
    </a>
  ))}
</div>
