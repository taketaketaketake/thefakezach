---
interface Props {
  email?: string;
  calendlyUrl?: string;
}

const {
  email = "zach@takedetroit.com",
  calendlyUrl = "#"
} = Astro.props;
---

<style>
  .contact-button {
    background: linear-gradient(45deg, #ff0080, #00ffff, #ff0080);
    background-size: 200% 200%;
    animation: button-glow 2s ease-in-out infinite alternate;
    position: relative;
    overflow: hidden;
  }

  .contact-button::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent);
    transition: left 0.5s;
  }

  .contact-button:hover::before {
    left: 100%;
  }

  .floating-code {
    position: absolute;
    font-family: 'Courier New', monospace;
    font-size: 12px;
    color: #00ffff;
    opacity: 0.6;
    animation: float-code 6s ease-in-out infinite;
  }

  @keyframes button-glow {
    0% { background-position: 0% 50%; box-shadow: 0 0 20px rgba(255, 0, 128, 0.5); }
    100% { background-position: 100% 50%; box-shadow: 0 0 30px rgba(0, 255, 255, 0.7); }
  }

  @keyframes float-code {
    0%, 100% { transform: translateY(0px) rotate(0deg); }
    25% { transform: translateY(-10px) rotate(2deg); }
    50% { transform: translateY(-5px) rotate(-1deg); }
    75% { transform: translateY(-12px) rotate(1deg); }
  }
</style>

<section class="w-full bg-transparent py-16 relative overflow-hidden">
  <!-- Floating Code Elements -->
  <div class="floating-code" style="top: 10%; left: 5%; animation-delay: 0s;">connect.now();</div>
  <div class="floating-code" style="top: 30%; right: 10%; animation-delay: 2s;">mission.start();</div>
  <div class="floating-code" style="bottom: 20%; left: 15%; animation-delay: 4s;">contact.send();</div>

  <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
    
    <!-- Section Header -->
    <div class="text-center mb-16">
      <div class="inline-flex items-center space-x-2 bg-green-900 bg-opacity-30 border border-green-400 rounded-full px-4 py-2 mb-6">
        <div class="w-2 h-2 bg-green-400 rounded-full animate-pulse"></div>
        <span class="text-green-400 text-sm font-semibold uppercase tracking-wide">COMMUNICATION CHANNELS OPEN</span>
      </div>
      
      <h2 class="text-4xl md:text-5xl lg:text-6xl font-bold text-white mb-4">
        ESTABLISH CONNECTION
      </h2>
      <p class="text-xl md:text-2xl text-cyan-400 font-semibold mb-8">
        Initiate Direct Communication Protocol
      </p>
    </div>

    <!-- Contact Cards -->
    <div class="grid grid-cols-1 md:grid-cols-2 gap-8 mb-12 max-w-4xl mx-auto">
      
      <!-- Email Contact -->
      <div class="border-2 border-pink-500 rounded-lg p-8 backdrop-blur-md bg-pink-500 bg-opacity-10 hover:bg-opacity-20 transition-all duration-300 relative group">
        <div class="text-4xl mb-4">📡</div>
        <h3 class="text-xl font-bold text-white mb-3">DIRECT TRANSMISSION</h3>
        <p class="text-gray-300 mb-6">Ready to discuss your project requirements and timeline.</p>
        <a 
          href={`mailto:${email}`}
          class="contact-button inline-block text-white font-bold py-3 px-6 rounded-lg text-lg transition-all duration-300 uppercase tracking-wide"
        >
          Send Message
        </a>
        <div class="absolute top-2 right-2 text-xs text-pink-400 font-mono">
          PRIORITY_1
        </div>
      </div>

      <!-- Calendar Booking -->
      <div class="border-2 border-cyan-400 rounded-lg p-8 backdrop-blur-md bg-cyan-400 bg-opacity-10 hover:bg-opacity-20 transition-all duration-300 relative group">
        <div class="text-4xl mb-4">🚀</div>
        <h3 class="text-xl font-bold text-white mb-3">MISSION BRIEFING</h3>
        <p class="text-gray-300 mb-6">Schedule a strategic consultation to align on objectives.</p>
        <a 
          href={calendlyUrl}
          target="_blank"
          rel="noopener noreferrer"
          class="contact-button inline-block text-white font-bold py-3 px-6 rounded-lg text-lg transition-all duration-300 uppercase tracking-wide"
        >
          Book Session
        </a>
        <div class="absolute top-2 right-2 text-xs text-cyan-400 font-mono">
          PRIORITY_1
        </div>
      </div>

    </div>

    <!-- Status Footer -->
    <div class="text-center">
      <div class="bg-black bg-opacity-60 border border-gray-600 rounded-lg px-6 py-4 inline-block">
        <div class="flex items-center space-x-4">
          <div class="flex items-center space-x-2">
            <div class="w-2 h-2 bg-green-400 rounded-full animate-pulse"></div>
            <span class="text-green-400 text-sm font-mono">SYSTEMS READY</span>
          </div>
          <div class="w-px h-4 bg-gray-600"></div>
          <div class="text-xs text-gray-400 font-mono">
            RESPONSE_TIME: &lt;24H
          </div>
          <div class="w-px h-4 bg-gray-600"></div>
          <div class="text-xs text-gray-400 font-mono">
            TIMEZONE: EST
          </div>
        </div>
      </div>
    </div>

  </div>
</section>